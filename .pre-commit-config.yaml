repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check --fix src tests
        language: system
        types: [python]

      - id: ruff-format
        name: ruff format
        entry: uv run ruff format src tests
        language: system
        types: [python]
